image: node:latest

before_script:
  - npm install

cache:
  paths:
    - node_modules/
    - .next/cache/

pages:
  script:
    - yarn install
    - yarn build
    - mv out public
  artifacts:
    paths:
      - public
  only:
    - main
